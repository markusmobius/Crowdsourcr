#apt-get install pip
#pip install -r requirements.txt

pymongo==2.5.2
tornado==3.1
boto==2.29.1

# Only if on linux
#python-daemon==1.6

# only if you want to be able to serve the docs at http://$SERVER/docs
#docutils
